[package]
name    = "Tara"
version = "0.1.0"
edition = "2021"

# Inline assembly requires nightly, OR you can use stable with
# `core::arch::asm!` which is stable since Rust 1.59.
[profile.release]
opt-level = 3